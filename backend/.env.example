# JobPilot backend environment
# Copy this file to backend/.env. Never commit real backend/.env files.

# Application
APP_NAME=JobPilot Backend
APP_ENV=local
APP_DEBUG=true

# Data stores
# These defaults match the repository-root .env.example and compose.yaml.
DATABASE_URL=postgresql+asyncpg://postgres:123456@127.0.0.1:25432/jobpilot
REDIS_URL=redis://127.0.0.1:26379/0

# OpenAI-compatible chat completions endpoint
# Required for AI parsing, drafting, matching, artifact generation, and assistant workflows.
LLM_BASE_URL=https://api.example.com/v1
LLM_API_KEY=replace-with-your-api-key
LLM_MODEL_NAME=replace-with-your-chat-model

# OpenAI-compatible BGE-M3 embeddings endpoint
# Leave base URL / API key blank only when the chat endpoint also serves embeddings.
# EMBEDDING_DIMENSIONS must match the database vector column dimension.
EMBEDDING_BASE_URL=
EMBEDDING_API_KEY=
EMBEDDING_MODEL_NAME=BAAI/bge-m3
EMBEDDING_DIMENSIONS=1024
# BGE-M3 has a fixed output dimension; most self-hosted endpoints reject this OpenAI extension.
EMBEDDING_SEND_DIMENSIONS=false

# RAG retrieval strategy: hybrid is the quality-first default; bm25 is the fast fallback.
RAG_STRATEGY=hybrid
RAG_CANDIDATE_MULTIPLIER=3
RAG_BM25_K1=1.5
RAG_BM25_B=0.75
RAG_HYBRID_RRF_K=60
RAG_VECTOR_WEIGHT=1.0
RAG_BM25_WEIGHT=1.0
RAG_HYBRID_VECTOR_FAIL_OPEN=true

# POST /rerank endpoint. Enabled by default; fail-open preserves Hybrid order.
RAG_RERANKER_ENABLED=true
RAG_RERANKER_FAIL_OPEN=true
RERANKER_BASE_URL=
RERANKER_API_KEY=
RERANKER_MODEL_NAME=BAAI/bge-reranker-v2-m3
RERANKER_TIMEOUT_SECONDS=15

# Authentication
# Local development can keep AUTH_DEV_MODE=true to allow X-User-Name fallback.
# Production must use AUTH_DEV_MODE=false and a long random AUTH_SECRET_KEY.
AUTH_SECRET_KEY=change-this-to-a-long-random-secret
AUTH_ALGORITHM=HS256
AUTH_ACCESS_TOKEN_EXPIRE_MINUTES=1440
AUTH_DEV_MODE=true

# MCP Client: connect JobPilot Assistant to administrator-approved remote servers.
# Keep credentials in separate environment variables and reference only their names
# from MCP_SERVERS_JSON. An empty allowed_tools list exposes nothing.
MCP_ENABLED=false
MCP_SERVERS_JSON=[]
MCP_DISCOVERY_TIMEOUT_SECONDS=5
MCP_CALL_TIMEOUT_SECONDS=15
MCP_DISCOVERY_CACHE_SECONDS=60
MCP_MAX_RESULT_CHARS=20000
# Example:
# JOB_SEARCH_MCP_TOKEN=replace-with-remote-server-token
# MCP_SERVERS_JSON=[{"id":"jobs","url":"https://jobs.example.com/mcp","category":"job_search","allowed_tools":["search_jobs","get_job_detail"],"auth_token_env":"JOB_SEARCH_MCP_TOKEN"}]

# JobPilot read-only MCP Server (run as a separate ASGI process).
MCP_SERVER_HOST=127.0.0.1
MCP_SERVER_PORT=8001
MCP_SERVER_PUBLIC_URL=http://127.0.0.1:8001/mcp
MCP_SERVER_ISSUER_URL=http://127.0.0.1:8000
MCP_TOKEN_EXPIRE_MINUTES=60

# Production checklist:
# APP_ENV=production
# APP_DEBUG=false
# AUTH_DEV_MODE=false
# AUTH_SECRET_KEY=<long random secret>
# DATABASE_URL=<production database URL>
# REDIS_URL=<production Redis URL>
# LLM_API_KEY=<production key>
# MCP_SERVER_PUBLIC_URL=<public HTTPS /mcp URL>
# MCP_SERVER_ISSUER_URL=<public HTTPS JobPilot API URL>
